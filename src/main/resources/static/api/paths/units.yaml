units:
  get:
    tags:
      - Units
    summary: "Retrieve all units."
    responses:

      "200":
        description: "Everything went fine"
        content:
          application/json:
            schema:
              type: array
              items:
                type: string

